# Save this file as UTF-8 without BOM (see root .env.example for PowerShell snippet).
DATABASE_URL="mysql://root:root_001@localhost:3310/base_default"
HOST=0.0.0.0
PORT=3010
LOG_LEVEL=info

ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
PASSWORD_PEPPER=change-me-pepper
RESET_TOKEN_TTL=20m
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Provide RSA key material via environment or allow the application to generate development keys.
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
CORS_ALLOWED_ORIGINS=http://localhost:3000
