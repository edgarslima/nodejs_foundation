# syntax=docker/dockerfile:1

FROM mysql:8.4

# The official MySQL image reads configuration from environment variables passed at runtime.
# No additional build steps are required; this Dockerfile exists to satisfy per-component image requirements.

EXPOSE 3306
